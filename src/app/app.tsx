import React from 'react';  // Add this line at the top

const App: React.FC = () => {
    return (
        <div>
            <h1>Hello Electron + React!</h1>
        </div>
    );
};

export default App;