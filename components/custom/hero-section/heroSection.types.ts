export interface MousePosition {
    x: number;
    y: number;
}

export interface GridConfig {
    dotSize: number;
    spacing: number;
    maxDistance: number;
    maxScale: number;
}

export interface HeroProps {
    className?: string;
}